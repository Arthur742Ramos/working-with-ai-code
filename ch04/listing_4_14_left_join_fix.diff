-    for item in items:
-        product = lookup_product(con, item["code"])
-        price = product["price"]
+    cur = con.execute(
+        "SELECT oi.code AS code, oi.qty AS qty,"
+        "       p.name AS name, p.price AS price"
+        "  FROM order_items oi"
+        "  LEFT JOIN products p"
+        "    ON p.code = oi.code"
+        " WHERE oi.order_id = ?",
+        (order_id,),
+    )
+    for item in cur.fetchall():
+        if item["price"] is None:
+            name = "(unknown product)"
+            price = 0.0
+        else:
+            name = item["name"]
+            price = item["price"]
